////  LoginView.swift//  PokéExplorer////  Created by user276508 on 14/06/25.//import SwiftUIimport SwiftDatastruct LoginView: View {    @Environment(\.modelContext) var context    @EnvironmentObject var authManager: AuthenticationManager    @StateObject private var viewModel = LoginViewModel()    var body: some View {        NavigationView {            VStack(spacing: 20) {                Text("PokéExplorer")                    .font(.largeTitle)                    .fontWeight(.bold)                TextField("Email", text: $viewModel.email)                    .textFieldStyle(RoundedBorderTextFieldStyle())                    .autocapitalization(.none)                    .keyboardType(.emailAddress)                SecureField("Senha", text: $viewModel.senha)                    .textFieldStyle(RoundedBorderTextFieldStyle())                if let erro = viewModel.erroLogin {                    Text(erro)                        .foregroundColor(.red)                        .font(.caption)                }                Button("Entrar") {                    if let usuarioAutenticado = viewModel.autenticar(contexto: context) {                        authManager.login(user: usuarioAutenticado)                    }                }                .buttonStyle(.borderedProminent)                NavigationLink("Cadastre-se", destination: CadastroView())            }            .padding()        }    }}