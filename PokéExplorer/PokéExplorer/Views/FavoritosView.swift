////  FavoritosView.swift//  PokéExplorer////  Created by user276508 on 14/06/25.//import SwiftUIimport SwiftDatastruct FavoritosView: View {    var usuario: Usuario    @Environment(\.modelContext) var context    @StateObject private var viewModel = FavoritosViewModel()    var body: some View {        NavigationView {            List {                ForEach(viewModel.favoritos) { favorito in                    HStack {                        AsyncImage(url: URL(string: favorito.imagemURL)) { image in                            image.resizable()                        } placeholder: {                            ProgressView()                        }                        .frame(width: 50, height: 50)                        .clipShape(RoundedRectangle(cornerRadius: 8))                        VStack(alignment: .leading) {                            Text(favorito.nome.capitalized)                                .font(.headline)                            Text("ID: \(favorito.pokemonId)")                                .font(.subheadline)                                .foregroundColor(.gray)                        }                    }                }                .onDelete { indexSet in                    deletarFavorito(at: indexSet)                }            }            .navigationTitle("Favoritos")            .onAppear {                viewModel.carregarFavoritos(usuario: usuario, contexto: context)            }        }    }    private func deletarFavorito(at offsets: IndexSet) {        for index in offsets {            let favorito = viewModel.favoritos[index]            viewModel.remover(favorito: favorito, contexto: context)        }    }}