////  CadastroView.swift//  PokéExplorer////  Created by user276508 on 14/06/25.//import SwiftUIimport SwiftDatastruct CadastroView: View {    @Environment(\.modelContext) var context    @Environment(\.dismiss) var dismiss    @StateObject private var viewModel = CadastroViewModel()    var body: some View {        NavigationView {            Form {                Section(header: Text("Crie sua conta")) {                    TextField("Nome de Usuário", text: $viewModel.nome)                        .autocapitalization(.none)                    TextField("Email", text: $viewModel.email)                        .autocapitalization(.none)                        .keyboardType(.emailAddress)                    SecureField("Senha", text: $viewModel.senha)                    SecureField("Confirmar Senha", text: $viewModel.confirmarSenha)                }                if let mensagemErro = viewModel.mensagemErro {                    Text(mensagemErro)                        .foregroundColor(.red)                }                Section {                    Button("Cadastrar") {                        viewModel.cadastrar(contexto: context)                    }                    .buttonStyle(.borderedProminent)                }            }            .navigationTitle("Cadastro")            .onChange(of: viewModel.cadastroConcluido) {                if viewModel.cadastroConcluido {                    dismiss()                }            }        }    }}