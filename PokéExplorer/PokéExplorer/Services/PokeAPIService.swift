////  PokeAPIService.swift//  PokéExplorer////  Created by user276508 on 14/06/25.//import Foundationclass PokeAPIService {    func fetchPokemonList(completion: @escaping (Result<[Pokemon], Error>) -> Void) {            let url = URL(string: "https://pokeapi.co/api/v2/pokemon?limit=50")!            URLSession.shared.dataTask(with: url) { data, response, error in                if let error = error {                    completion(.failure(error))                    return                }                guard let data = data else {                    completion(.failure(NSError(domain: "Sem dados", code: 0)))                    return                }                do {                    let decoded = try JSONDecoder().decode(PokemonListResponse.self, from: data)                    let pokemons: [Pokemon] = decoded.results.enumerated().map { index, apiResult in                        let id = index + 1 // Assume ordem da lista como ID                        return Pokemon(                            id: id,                            name: apiResult.name,                            imageURL: "https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/\(id).png",                            height: 0,                            weight: 0,                            types: [],                            abilities: []                        )                    }                    completion(.success(pokemons))                } catch {                    completion(.failure(error))                }            }.resume()        }}